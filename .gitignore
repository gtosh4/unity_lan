# Rust
/target
**/*.rs.bk
*.pdb

# wireguard-nt runtime DLL, staged locally for defguard_wireguard_rs (fetched per-machine,
# not committed). See scripts/dev-run.ps1.
/resources-windows

# Packaging build output
/packaging/dist

# Local secrets / state
*.db
*.db-*
/secrets
coordinator.toml
*.key
# Local engine dev state (WG key, device token, control socket) + local config
/engine-state
/engine-state-local
/engine-state-prod
/engine.toml
/engine.local.toml
/engine.prod.toml
